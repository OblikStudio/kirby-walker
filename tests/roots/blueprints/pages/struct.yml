title: Struct
preset: page
fields:
  itemsSync:
    type: structure
    fields:
      id:
        type: text
      num:
        type: number
    outsource:
      sync: id
  itemsSyncNested:
    type: structure
    fields:
      struct:
        type: structure
        fields:
          text: true
        outsource:
          sync: id
    outsource:
      sync: id
