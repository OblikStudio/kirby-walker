title: Struct
preset: page
fields:
  itemsSync:
    type: structure
    fields:
      struct:
        type: structure
        fields:
          text: true
        outsource:
          sync: id
    outsource:
      sync: id
