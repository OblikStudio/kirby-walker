title: Struct
preset: page
fields:
  items:
    type: structure
    fields:
      text: true
  itemsSync:
    type: structure
    fields:
      id:
        type: text
      num:
        type: number
    outsource:
      sync: id
  itemsSyncNested:
    type: structure
    fields:
      struct:
        type: structure
        fields:
          text: true
        outsource:
          sync: id
    outsource:
      sync: id
